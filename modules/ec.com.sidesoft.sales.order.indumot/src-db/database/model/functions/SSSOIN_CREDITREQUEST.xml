<?xml version="1.0"?>
  <database name="FUNCTION SSSOIN_CREDITREQUEST">
    <function name="SSSOIN_CREDITREQUEST" type="NULL">
      <parameter name="p_pinstance_id" type="VARCHAR" mode="in">
        <default/>
      </parameter>
      <body><![CDATA[TYPE RECORD IS REF CURSOR;
    Cur_Parameter RECORD;
    Cur_Lines RECORD;

    v_User_ID VARCHAR2(32) ;
    v_RecordId VARCHAR2(32);
    v_ResultStr VARCHAR2(2000):='';
    v_isquotation NUMBER;
    v_action VARCHAR2(200);
    v_justification VARCHAR2(200);

BEGIN

  --  Update AD_PInstance
  DBMS_OUTPUT.PUT_LINE('Updating PInstance - Processing ' || p_PInstance_ID) ;
  v_ResultStr := 'PInstanceNotFound';
  AD_UPDATE_PINSTANCE(p_PInstance_ID, NULL, 'Y', NULL, NULL) ;

  BEGIN --BODY
-- GET:    
    IF (p_PInstance_ID IS NOT NULL) THEN
      --  Update AD_PInstance
      DBMS_OUTPUT.PUT_LINE('Updating PInstance - Processing ' || p_PInstance_ID) ;
      v_ResultStr:='PInstanceNotFound';
      AD_UPDATE_PINSTANCE(p_PInstance_ID, NULL, 'Y', NULL, NULL) ;
      -- Get Parameters
      v_ResultStr:='ReadingParameters';
      FOR Cur_Parameter IN
        (SELECT i.Record_ID, i.AD_User_ID, p.ParameterName, p.P_String,
          p.P_Number, p.P_Date
        FROM AD_PINSTANCE i
        LEFT JOIN AD_PINSTANCE_PARA p
          ON i.AD_PInstance_ID=p.AD_PInstance_ID
        WHERE i.AD_PInstance_ID=p_PInstance_ID
        ORDER BY p.SeqNo
        )
      LOOP
        v_RecordId:=Cur_Parameter.Record_ID;
        v_User_ID:=Cur_Parameter.AD_User_ID;
        IF Cur_Parameter.ParameterName = 'em_sssoin_cred_req' THEN
            v_justification := Cur_Parameter.P_String;
        END IF;
      END LOOP; -- Get Parameter
    ELSE
      v_RecordId:=p_Order_ID;
      SELECT CREATEDBY INTO v_User_ID  FROM C_ORDER  WHERE C_ORDER_ID=p_Order_ID;
    END IF;
    DBMS_OUTPUT.PUT_LINE('  Record_ID=' || v_RecordId) ; 
    
    update c_order set EM_Sssoin_Generalstatus = 'CA', em_sssoin_cred_req = v_justification,
    EM_Sssoin_Creditreq = now(), EM_Sssoin_Creditreqby = v_User_ID
    where c_order_id = v_RecordId; 
    
    AD_UPDATE_PINSTANCE(p_PInstance_ID, NULL, 'N', 1, '@Success@') ;
    RETURN;

    
  END; --BODY
  EXCEPTION
  WHEN OTHERS THEN
    v_ResultStr:= '@ERROR=' || SQLERRM;
    DBMS_OUTPUT.PUT_LINE(v_ResultStr) ;
    AD_UPDATE_PINSTANCE(p_PInstance_ID, NULL, 'N', 0, v_ResultStr) ;
    RETURN;
END SSSOIN_CREDITREQUEST
]]></body>
    </function>
  </database>
